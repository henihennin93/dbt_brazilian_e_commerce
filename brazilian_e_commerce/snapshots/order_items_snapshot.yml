snapshots:
  - name: order_items_snapshot
    relation: ref('stg_order_items')
    config:
      schema: snapshots
      strategy: check
      unique_key:
        - order_id
        - order_item_id
      check_cols:
        - product_id
        - seller_id
        - shipping_limit_date
        - price
        - freight_value